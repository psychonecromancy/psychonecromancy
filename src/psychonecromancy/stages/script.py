"""Stage 3 — scene decomposition: brief.json -> shots.json.

Each shot needs an image generation prompt and a narration line. Schema
not yet decided — see docs/02-architecture.md.
"""


def run(brief: dict) -> list[dict]:
    raise NotImplementedError
