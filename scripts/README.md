# scripts/

Health checks, FFmpeg helpers, and export tooling supporting the pipeline
(e.g. a script to check ComfyUI is reachable, or to bulk-export n8n
workflows). Not the pipeline itself — pipeline orchestration lives in
`workflows/` (n8n) and `comfy/` (ComfyUI graphs).

Empty for now. Populate as Phase 1+ needs concrete tooling.
