# samples/

Reference outputs — good and bad — used to calibrate quality as the
pipeline is built. When you add a sample, note what input string produced
it, which prompt version (see `../prompts/`) and which ComfyUI graph
version (see `../comfy/`) generated it, so a later regression can be traced
to what changed.

Empty until there's something to put here.
